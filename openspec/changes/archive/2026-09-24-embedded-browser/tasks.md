## 1. Спайки (spikes/, отдельные модули; результаты — в design.md)

- [x] 1.1 S1: запуск Firefox ESR с `-profile` без `-no-remote` рядом с установленным Firefox; передача URL в запущенный экземпляр; поиск окна по образу процесса; привязка владельца (`GWLP_HWNDPARENT`) к окну Fyne через `RunNative`; сворачивание вместе, нет кнопки на панели задач; Process Monitor — нет записей вне папки приложения
- [x] 1.2 S2: распаковка установщика Firefox ESR через `/ExtractDir=` без установки и записи в реестр; запасной путь через 7z
- [x] 1.3 S3: очистка истории без потери закладок (sanitizeOnShutdown headless / удаление `places.sqlite` с восстановлением из `bookmarkbackups`)
- [x] 1.4 S4: адрес страницы загрузки — `Zone.Identifier` (`ReferrerUrl`), иначе BiDi `downloadWillBegin`; решение о судьбе требования «Источник загрузки»

## 2. Сборка дистрибутива

- [x] 2.1 `make browser`: загрузка закреплённой версии ESR (ru, win64), проверка SHA-256, распаковка в `dist/MangaReader/browser/firefox`; `browser/` в `.gitignore`
- [x] 2.2 `build-windows` в `dist/MangaReader/`, `package-windows` — zip папки; README: структура папки, размер

## 3. Модель и ссылка из meta.json

- [x] 3.1 `Gallery.SourceURL`; `meta.go`: поля `url`, `source`, `link`, `source_url`, `gallery_url`, первое корректное `http(s)`; тесты сценариев спеки
- [x] 3.2 `storage.FS.SourceURL` (Windows): `ReferrerUrl` из `Zone.Identifier`, только `http(s)`; сканер заполняет `SourceURL`, если в `meta.json` ссылки нет; тесты приоритета и файла без метки

## 4. Профиль и политики

- [x] 4.1 `browser/config.go`: `Policies`, `UserJS`, `UserChrome` из `Config`; golden-тесты; `browser` в `archtest`
- [x] 4.2 Проверка вручную (интеграционный тест + скриншоты; добавлены `SkipTermsOfUse` и `browser.preonboarding.enabled=false` — иначе окно «Условия использования»): нет приветственных страниц, загрузка без вопроса в `manga`, тёмная тема при светлой Windows, шапка без вкладок, `target=_blank` в том же окне

## 5. Процесс и окно (Windows)

- [x] 5.1 `browser.Browser`: `Open(url)` (запуск или передача URL), `Running()`, ошибка «Браузер не найден: <путь>», запись конфигурации перед запуском
- [x] 5.2 `window_windows.go`: поиск окон по образу процесса, привязка владельца и снятие `WS_EX_APPWINDOW`, цикл 500 мс для новых окон, вывод на передний план
- [x] 5.3 Закрытие при выходе: `WM_CLOSE`, ожидание 5 с, завершение процесса; вызов из `SetOnStopped`
- [x] 5.4 `CleanRegistry` (D9): значения с префиксом пути установки, новые пустые ключи `Installer`; вызов после выхода браузера и при запуске приложения; тест на временном ключе реестра

## 6. UI

- [x] 6.1 Кнопка «Браузер» на панели библиотеки (Windows); состояние через `fyne.Do`
- [x] 6.2 Раздел «Браузер» в настройках: домашняя страница, поисковик, «Расширения» (`about:addons`), подсказка о перезапуске; скрыт на Android
- [x] 6.3 Очистка cookies и истории (D5): настройка `browser.clear` → `privacy.sanitize.pending` в `user.js` один раз; подтверждение, предложение закрыть браузер, toast «Будет очищено при следующем открытии браузера»
- [x] 6.4 Кнопка «Открыть в браузере» на странице произведения: Windows → `browser.Open`, Android → `OpenURL`; скрыта без ссылки; тесты страницы

## 7. Проверка

- [x] 7.1 `make test` зелёный
- [x] 7.2 Ручной сценарий на Windows: скачать корректный архив и HTML-«архив» → галерея и ошибка; закладка переживает перезапуск; расширение устанавливается и остаётся; очистка cookies и истории; выход из приложения закрывает браузер; установленный Firefox не затронут
- [x] 7.3 README: раздел «Браузер» (где хранится, что отключено, как очистить, размер)

## 8. Доработки после проверки пользователем

- [x] 8.1 Спайк S5 (`spikes/firefox-extension`): расширение в профиле, связь по 127.0.0.1, адрес страницы при обрезанном Referer, работа без `policies.json` — результаты в design.md
- [x] 8.2 Настройки без политик (D2): `UserJS` со всеми заменами политик, удаление старого `policies.json`, шапка с «Все вкладки» / «Новая вкладка» / «MangaReader», новые вкладки вместо текущей; golden-тесты
- [x] 8.3 Расширение (D10): `internal/browser/ext` (manifest, bg.js), сборка xpi с `config.js`; тест содержимого архива
- [x] 8.4 Мост (D10): сервер на 127.0.0.1 с токеном, `/download` (проверка пути, запись `:mangareader.source`), `/show`; тесты (без токена — 403, путь вне папки — отказ, запись потока)
- [x] 8.5 `storage.FS.SourceURL`: сначала `:mangareader.source`, затем `ReferrerUrl`; `Source.Invalidate(relPath)`; тесты
- [x] 8.6 UI: запуск моста, `OnDownload` → Invalidate + RequestScan, `OnShow` → главное окно на передний план; «Поисковик» → `about:preferences#search` вместо выбора в приложении
- [x] 8.7 Интеграционный тест: загрузка с обрезанным Referer и перенаправлением → ссылка на страницу; `policies.json` удаляется (надпись об организации — проверена в S5); кнопка «MangaReader» сворачивает браузер — проверена в S5 кликом

