## 1. Данные и геометрия

- [x] 1.1 `library.Source.PageSizes(key)`: один `openZip`, `image.DecodeConfig` по страницам, ошибка страницы не прерывает обработку; тесты: `example.zip`, битая страница, неизвестная галерея
- [x] 1.2 `pages/fit.go`: `Fit`, `FitWidth`, `ZoomAt` (фокус точки), `ClampOffset`, префиксные суммы и `TopPage` (бинарный поиск); табличные тесты
- [x] 1.3 `pages/slice.go`: `Slice(img, maxH)` — независимые куски, суммарная высота = исходной; тест на 800×20000 и на изображение ниже порога

## 2. Загрузчик страниц (`internal/pages`)

- [x] 2.1 `Loader`: запрос `{key, page, targetW, targetH}`, декодирование + уменьшение (не больше оригинала), округление размера до 64 px в ключе кэша
- [x] 2.2 LRU по байтам (лимит задаётся), пул воркеров, приоритет текущей страницы, отбрасывание устаревших поколений, `Clear()`
- [x] 2.3 Тесты под `-race`: кэш-хит без повторного декодирования, вытеснение по байтам, устаревший запрос не декодируется, битая страница → ошибка; `pages` в `archtest`

## 3. Слой читалки и клавиши

- [x] 3.1 `reader.Reader`: слой с непрозрачным фоном, `Open(g)` / `Close()` / `Visible()`, `Unfocus` при открытии, очистка кэша страниц при закрытии, режим из Preferences
- [x] 3.2 Оболочка: `Stack(tabs, reader, toast)`, открытие по нажатию на карточку (`library.go`: снятие выделения, без toast)
- [x] 3.3 `Canvas.SetOnTypedKey`: Esc/Back закрывают читалку, клавиши навигации — в читалку, Back вне читалки → `GoBack` через интерфейс

## 4. Постраничный режим

- [x] 4.1 `pagedView`: отображение вписанной страницы, индикатор загрузки, сообщение об ошибке, запрос текущей и предзагрузка ±1
- [x] 4.2 Навигация: тап-зоны (трети), свайп с порогом, колесо с ограничением частоты, клавиши, Home/End, границы книги
- [x] 4.3 Двойной тап ×2 с фокусом, перетаскивание с ограничением краёв, сброс при смене страницы, запрос версии высокого разрешения при увеличении

## 5. Режим ленты

- [x] 5.1 `stripView`: `container.Scroll` + `stripContent` с высотами из `PageSizes`, размещение только видимых страниц (окно −H…+2H), пул объектов
- [x] 5.2 Загрузка страниц по ширине, резка > 2048 физ. px на куски, размещение по целым пикселям, применение с проверкой актуальности
- [x] 5.3 Текущая страница по `OnScrolled` (`TopPage`), переход к странице, пересчёт высот при смене ширины с сохранением страницы, тап — панель

## 6. Панель и режимы

- [x] 6.1 Панель: название, «N / всего», ползунок перехода, переключатель «Страницы / Лента», «Закрыть»; скрыта при открытии, переключается тапом
- [x] 6.2 Переключение режима с сохранением текущей страницы; запись режима в Preferences

## 7. Проверка

- [x] 7.1 `make test` проходит, включая `-race` для `internal/library` и `internal/pages`
- [x] 7.2 Windows: открыть `example.zip`, листать тапами/клавишами/колесом, двойной клик ×2 и перетаскивание, Esc; лента на сгенерированной книге из 20 страниц и странице 800×20000; переключение режима и его сохранение после перезапуска; возврат в библиотеку без сброса прокрутки
- [x] 7.3 Android (эмулятор): свайпы, двойной тап, лента, «Назад» в читалке закрывает её, «Назад» в библиотеке сворачивает приложение
- [x] 7.4 Память: лента из 100 страниц 1200×1700 — прокрутка до конца, рабочий набор процесса не растёт неограниченно; записать результат в design.md
