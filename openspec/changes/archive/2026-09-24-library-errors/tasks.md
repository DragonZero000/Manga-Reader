## 1. Сканер: все файлы и незавершённые загрузки

- [x] 1.1 `ScanError` с `Size` и `ModTime`; заполнение из `cacheEntry`; обновить тесты сканера
- [x] 1.2 `Scan`: сбор всех `Entry`, множество путей; пропуск `*.part` и пустого файла с соседним `<имя>.part` (без записи в кэш); тесты: идёт загрузка, загрузка завершилась
- [x] 1.3 Обработка не-`.zip` файлов как ошибок, `ErrEmpty` для пустого файла; тесты: `notes.txt`, `empty.zip`, скрытые файлы по-прежнему пропускаются

## 2. Классификация по содержимому

- [x] 2.1 `library/sniff.go`: `Kind`, `Sniff(File) Kind` (512 байт, `http.DetectContentType` + сигнатуры RAR, 7z, MP4/MOV, Matroska); табличные тесты на маленьких сгенерированных файлах
- [x] 2.2 `UnsupportedError{Kind}` с текстами причин из спеки; `.zip` с ошибкой `zip.NewReader` → sniff → `UnsupportedError` или `ErrNotZip`; тесты: JPEG как `.zip`, HTML как `.zip`, обрезанный zip, `clip.mp4`

## 3. Пакет `internal/problems`

- [x] 3.1 `Tracker`: `Sync`, `Items` (сортировка), `Unseen`, `MarkAllSeen`; ключ «путь+размер+время»; `problems` в `archtest`
- [x] 3.2 Сохранение `errors.state` (ключ → просмотрена) в `storage.Settings` (JSON, запись только при изменении, обрезка до текущих ключей); тесты с `MemSettings`: перезапуск, файл удалён, файл исправлен, файл перекачан и снова ошибочен, без дублей; `-race`

## 4. UI

- [x] 4.1 `app.Services.Problems`; `Library.applyScan`: `Sync`, toast «Новые ошибки: N — см. вкладку «Ошибки»» только при `added>0`
- [x] 4.2 `ui/badge.go`: `BadgeIcon(base, n)` — SVG с кружком и числом, «99+», кэш по n; тест рендера в тестовом драйвере
- [x] 4.3 `screens.Errors`: список (маркер, путь, причина, время), выделение непросмотренных, пустое состояние «Ошибок нет»
- [x] 4.4 Оболочка: вкладка «Ошибки» между «Поиск» и «Настройки»; выбор вкладки → `MarkAllSeen`; после скана при открытой вкладке → `MarkAllSeen`; обновление иконки и `Tabs.Refresh()`
- [x] 4.5 Тесты оболочки: число на вкладке после скана, исчезает после открытия вкладки и после удаления файла; toast не повторяется при повторном скане

## 5. Проверка

- [x] 5.1 `make test` зелёный; ручная проверка на Windows: картинка, видео, HTML с расширением `.zip`, `.part`-файл, удаление файла
- [x] 5.2 Ручная проверка на Android: папка через SAF с картинкой и битым архивом; число на нижней вкладке; статус после перезапуска
- [x] 5.3 README: раздел «Ошибки» (что считается ошибкой, файлы не удаляются, скрытые файлы игнорируются)
