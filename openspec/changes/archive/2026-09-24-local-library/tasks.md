## 1. Разбор архива (`internal/library`)

- [x] 1.1 Тестовые хелперы: генерация zip-архивов во временной папке (со страницами, с/без `meta.json`, битый `meta.json`, только `meta.json`, не-zip файл, 12 страниц, `__MACOSX`)
- [x] 1.2 `meta.go`: структура `meta.json` с `flexInt` для `id`, перенос в `Gallery` (название через `ChooseTitle`, дата из Unix-секунд, теги через `AddTag`); тесты
- [x] 1.3 `archive.go`: `ReadArchive(path, relPath)` — поиск наименее вложенного `meta.json`, отбор страниц по расширению и исключениям, натуральная сортировка, `FileInfo`; предупреждение при битом `meta.json`; ошибки «не zip» и «нет изображений»
- [x] 1.4 Тест на `testdata/example.zip`: название, альт. название, ID 535147, 2 страницы, обложка, 8 тегов, дата 2024-10-15 UTC; тесты остальных сценариев спецификации `library-scan`

## 2. Сканер и источник

- [x] 2.1 `scanner.go`: обход `WalkDir`, фильтр `.zip` без учёта регистра, пропуск имён на `.`, ключ из относительного пути со слешами, сбор ошибок, сортировка (mtime ↓, ключ натурально)
- [x] 2.2 Инкрементальность: кэш по `(size, modTime)`, результат с `Added/Changed/Removed`; тест, что неизменённый архив не открывается повторно (счётчик открытий через подменяемую функцию)
- [x] 2.3 `source.go`: `Source` — `Scan(ctx)` с защитой от параллельного запуска (`ErrScanInProgress`), `Galleries()`, `Get(key)`, `OpenPage(key, page)` (чтение страницы в буфер), вызовы `Index.Upsert/Remove` с логированием ошибок
- [x] 2.4 Тесты `Source`: открытие обложки `example.zip` (байты совпадают), неизвестная страница/галерея → ошибка, параллельные `OpenPage` под `-race`, повторный `Scan` во время сканирования

## 3. Миниатюры (`internal/thumbs`)

- [x] 3.1 Декодирование JPEG/PNG/GIF/WebP (регистрация `x/image/webp`), уменьшение до 320 px (`x/image/draw`), `x/image` — прямая зависимость
- [x] 3.2 LRU-кэш на 300 записей по `key+modTime`, пул воркеров `min(4, NumCPU)`, объединение одинаковых запросов, колбэк с результатом или ошибкой
- [x] 3.3 Тесты: уменьшение сохраняет пропорции, повторный запрос берётся из кэша без декодирования, битое изображение → ошибка, вытеснение LRU

## 4. Экран «Библиотека»

- [x] 4.1 `app.Services`: `Library *library.Source`, `Thumbs *thumbs.Cache`; добавить `library` и `thumbs` в `archtest`
- [x] 4.2 Панель: счётчик галерей, кнопка «Обновить» с индикатором, блокировка во время сканирования; сканирование в горутине, применение через `fyne.Do`
- [x] 4.3 `widget.GridWrap`: карточка (обложка-заглушка → миниатюра / значок ошибки, название в 2 строки), защита от применения миниатюры к переиспользованной карточке
- [x] 4.4 Пустое состояние с путём (переиспользовать текущее), переключение между сеткой и пустым состоянием; toast «Не удалось прочитать архивов: N» и список ошибок в лог
- [x] 4.5 Нажатие на карточку → toast «<название> — N стр.», снятие выделения
- [x] 4.6 Автосканирование: `Lifecycle().SetOnStarted` и `SetOnEnteredForeground` с троттлингом 2 с

## 5. Проверка

- [x] 5.1 `make test` проходит, включая `go test -race ./internal/library/... ./internal/thumbs/...`
- [x] 5.2 Windows: положить `example.zip` и 1 битый архив в папку библиотеки → карточка с обложкой и названием «english name», toast об 1 ошибке; «Обновить» после добавления копии архива показывает её первой; нажатие на карточку → toast
- [x] 5.3 Android (эмулятор): `adb push` архива, возврат в приложение → карточка появляется без «Обновить»; нижние вкладки и сетка на узком экране
- [x] 5.4 Производительность: 300 сгенерированных архивов — первое сканирование и прокрутка сетки без зависаний UI; записать время сканирования в design.md
