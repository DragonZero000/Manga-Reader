## 1. Настройка режима

- [x] 1.1 В `internal/app` добавить `KeySearchMode = "search.mode"`, константы `SearchModeDynamic`/`SearchModeSubmit` и `SearchMode(storage.Settings) string` (неизвестное значение → `dynamic`); тест на значение по умолчанию и разбор

## 2. Экран поиска

- [x] 2.1 В `Search` добавить состояние из design D1 (`mode`, `delay` = 500 мс, `last`, `timerGen`, `filling`, `helpDone`) и тестовый хук `SetDelay`; режим читать из `svc.Settings` в `NewSearch`
- [x] 2.2 Разделить `Run` на `execute(text)` (пустой текст — no-op, `last = text`, разбор, фон, `seq`) и точки входа: таймер, 🔍/Enter в режиме «По кнопке», `Rerun()` → `execute(last)`, `SetQuery` (с `filling` и отменой таймера)
- [x] 2.3 Таймер с `timerGen` и `cancelTimer` (design D3); `OnChanged` запускает таймер только в режиме «При вводе» и при `!filling`
- [x] 2.4 `OnSubmitted`: в режиме «При вводе» снять фокус с поля (канвас через `CanvasForObject`, nil-safe), в режиме «По кнопке» — `execute(entry.Text)`
- [x] 2.5 Справка: показывается только до первого `execute`; убрать показ справки при пустом поле; `SetItems(nil)` при пустом поле больше не вызывается
- [x] 2.6 Кнопка ✕ (`theme.ContentClearIcon`) и метод `Clear()` (отмена таймера, очистка поля без изменения статуса и сетки); раскладка `Border(…, HBox(✕, 🔍), entry)`
- [x] 2.7 `SetMode(m)`: отмена таймера, показать/скрыть 🔍

## 3. Настройки и оболочка

- [x] 3.1 В `internal/ui/screens/settings.go` карточка «Поиск» с `RadioGroup` «При вводе» / «По кнопке» и подписью; сохранение в `KeySearchMode`, колбэк `onSearchMode`
- [x] 3.2 В `internal/ui/shell.go` передать `s.search.SetMode` в `NewSettings`; убедиться, что `SearchFor` и `Rerun` работают по новым правилам

## 4. Тесты

- [x] 4.1 Обновить `internal/ui/search_test.go` под `SetDelay(50ms)`: справка сразу после создания; `SetQuery("")` ничего не меняет; debounce с перезапуском таймера (`ком` → пауза < delay → `комикс`)
- [x] 4.2 Тесты режима «По кнопке»: ввод без 🔍 не выполняет запрос; 🔍/Enter выполняет; ошибка разбора не показывается до выполнения
- [x] 4.3 Тесты ✕: результаты и «Найдено: N» сохраняются; ожидающий запрос отменяется
- [x] 4.4 Тесты: после первого запроса пустое поле и возврат на вкладку не показывают справку; `Rerun` выполняет `last`, а не неотправленный текст; `SearchFor` в режиме «При вводе» выполняет запрос один раз; `SetMode` отменяет ожидание
- [x] 4.5 Тест настроек: по умолчанию выбран «При вводе», выбор сохраняется в `search.mode` и вызывает колбэк

## 5. Документация

- [x] 5.1 `docs/ru/user-guide.md` и `docs/en/user-guide.md`: раздел «Поиск» (два режима, задержка 500 мс, кнопка ✕, справка только до первого запроса) и раздел «Настройки» (карточка «Поиск»)

## 6. Проверка

- [x] 6.1 `make test` проходит; ручная проверка на Android: Enter на экранной клавиатуре (закрывает клавиатуру в «При вводе», запускает поиск в «По кнопке»), результаты при открытой клавиатуре, ✕
