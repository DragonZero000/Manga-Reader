## 1. Наблюдатель

- [x] 1.1 `fsnotify` — прямая зависимость в `go.mod`; `library/watch.go`: `NewWatcher(root)`, рекурсивное добавление папок (без скрытых), добавление новых папок на `Create`
- [x] 1.2 Объединение событий: тишина 1 с, предел 10 с, фильтр записи в `*.part`; интервалы задаются для тестов
- [x] 1.3 Тесты во временной папке: создание файла, новая вложенная папка с файлом, удаление, серия из 50 файлов → один вызов, запись в `.part` → нет вызова, rename `.part` → вызов

## 2. Занятые файлы

- [x] 2.1 `storage.ErrBusy`: `busy_windows.go` оборачивает `ERROR_SHARING_VIOLATION`/`ERROR_LOCK_VIOLATION`; на других ОС — заглушка
- [x] 2.2 Сканер: `ErrBusy` не кэшируется и не попадает в ошибки, счётчик занятых в `ScanResult`; режим «занятые — в ошибки» с причиной «Файл занят другой программой»; тесты с подменой хранилища

## 3. Экран и оболочка

- [x] 3.1 `Library.RequestScan` с флагом `pending` и повтором после `applyScan`; повторы при занятых файлах (2 с, 5 раз)
- [x] 3.2 `Services.Watcher` (ПК), запуск в `SetOnStarted`, остановка в `SetOnStopped`, вызов через `fyne.Do`; ошибка запуска — только лог
- [x] 3.3 Тесты экрана: событие во время скана → ещё один скан; не более одного скана одновременно

## 4. Проверка

- [x] 4.1 `make test` зелёный (`-race` для наблюдателя)
- [x] 4.2 Ручная проверка на Windows: копирование архива, скачивание файла Firefox в `manga` (сымитировано: заглушка + `.part` → переименование; реальная загрузка — в `embedded-browser`), удаление, новая вложенная папка; массовое копирование — автотестом
- [x] 4.3 README: библиотека обновляется сама на ПК
