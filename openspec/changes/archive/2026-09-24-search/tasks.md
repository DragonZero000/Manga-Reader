## 1. Модель и парсер (`internal/search`)

- [x] 1.1 `Query.Terms`; `Validate` сохраняет пустой тип тега (нормализуя имя); обновить тесты модели (тег без типа)
- [x] 1.2 `Parse`: токенизация (пробелы, кавычки, незакрытая кавычка), слова в нижнем регистре, распознавание полей без учёта регистра, `-` только для тегов, `*ParseError`
- [x] 1.3 Значения: числа (`N`, `>`, `<`, `>=`, `<=`, `N..M`), размеры с единицами и дробной частью, даты-периоды (`ГГГГ[-ММ[-ДД]]`, операторы, диапазоны; `uploaded` — UTC, `added` — местное время); валидация результата
- [x] 1.4 Табличные тесты парсера по всем сценариям `search-syntax`

## 2. Индекс в памяти

- [x] 2.1 `MemIndex`: `doc` с подготовленными строками, тегами и именами тегов; `Upsert`/`Remove` под `RWMutex`
- [x] 2.2 `Search`: слова (подстрока в любом поле, «И»), фильтры по правилам `search-index`, нулевые значения не проходят, сортировка с натуральным порядком ключей, `Limit/Offset`, `total`
- [x] 2.3 `SuggestTags`: префикс, тип, счётчики, порядок, лимит
- [x] 2.4 Тесты по сценариям `search-index` на `example.zip` и сгенерированных галереях; тест одновременных `Upsert` и `Search` под `-race`
- [x] 2.5 `internal/app`: `MemIndex` вместо `NopIndex`; тест, что после `Source.Scan` поиск находит `example.zip`

## 3. Экраны

- [x] 3.1 Вынести сетку карточек в `screens/grid.go` (`galleryGrid`: `SetItems`, `onOpen`, миниатюры); библиотека использует её без изменения поведения
- [x] 3.2 Экран «Поиск»: поле, статус, сетка результатов, справка при пустом запросе, дребезг 300 мс и Enter, отбрасывание устаревших результатов, ошибки разбора под полем с сохранением прежних результатов, `SetQuery`, `Rerun`
- [x] 3.3 Оболочка: результаты открывают страницу произведения; повторный поиск при выборе вкладки и после сканирования (`Library.onScanned`); `searchFor(text)` — закрыть страницу произведения, выбрать вкладку «Поиск», `SetQuery`
- [x] 3.4 Страница произведения: теги — кнопки, нажатие вызывает поиск `тип:"имя"` / `tag:"имя"`

## 4. Проверка

- [x] 4.1 UI-тесты (подменяемый `do`): ввод запроса показывает «Найдено: 1»; ошибка разбора показывается, результаты сохраняются; пустой запрос — справка; нажатие на тег на странице произведения открывает поиск с `artist:"artist 1"`
- [x] 4.2 `make test` проходит, включая `-race` для `internal/search` и `internal/ui/...`
- [x] 4.3 Windows: библиотека из `example.zip`, книги на 20 страниц, вебтуна и 300 сгенерированных архивов — поиск по слову, `pages:>10`, `tag:"tag 1"`, `-tag:`, `uploaded:2024`, ошибка разбора; время поиска на 300 архивах записать в design.md
- [x] 4.4 Android (эмулятор): ввод запроса с экранной клавиатуры, результаты, тег → поиск, «Назад»
