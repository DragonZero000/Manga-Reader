## 1. Форматирование сведений

- [x] 1.1 `details/format.go`: `TagGroups` (порядок известных типов, неизвестные по алфавиту, пустые группы не возвращаются) и русские подписи типов; тесты на `example.zip`, неизвестный тип, пустой список
- [x] 1.2 `InfoRows`: единое правило — поле с пустым значением (пустая/пробельная строка, 0, нулевая дата) не возвращается, без исключений; «Страниц: N (в метаданных: M)», путь относительно библиотеки; `FormatSize` (Б/КБ/МБ/ГБ, запятая), `FormatDate` (UTC-дата), `FormatDateTime`, `FormatInt` (разряды); табличные тесты, включая эталонный архив, архив без метаданных, размер 0, сканлейтор из пробелов

## 2. Экран произведения

- [x] 2.1 `details.Details`: слой с непрозрачным фоном, верхняя полоса (кнопка «Назад» + название), `Open(g)` / `Close()` / `Visible()`, `Unfocus` при открытии, прокрутка в начало, перерисовка через canvas окна
- [x] 2.2 Содержимое: обложка, основное и альтернативное название, кнопка «Читать», группы тегов с чипами в `RowWrapLayout`, сведения в `FormLayout`; скрытие пустых разделов
- [x] 2.3 Виджет обложки: миниатюра из `thumbs.Cache` → изображение из собственного `pages.Loader` (32 МБ, 1 воркер), вписывание по ширине с ограничением 60% высоты окна, индикатор, значок ошибки, пересчёт при изменении размера, тап → читалка; очистка кэша при закрытии

## 3. Навигация

- [x] 3.1 Оболочка: `Stack(tabs, details, reader, toast)`; карточка библиотеки открывает страницу произведения; «Читать» и обложка открывают читалку
- [x] 3.2 `Shell.typedKey`: цепочка Esc/Back — читалка → страница произведения → сворачивание приложения

## 4. Проверка

- [x] 4.1 UI-тесты на тестовом драйвере (с подменой `do`): открытие показывает название, теги и сведения `example.zip`; «Читать» открывает читалку; Esc закрывает сначала читалку, затем страницу произведения; архив без `meta.json` — без раздела тегов
- [x] 4.2 `make test` проходит, включая `-race` для `internal/ui/...`
- [x] 4.3 Windows: карточка → страница произведения (скриншот), «Читать» → читалка → Esc → страница → Esc → библиотека с прежней прокруткой; битая обложка и высокая обложка
- [x] 4.4 Android (эмулятор): страница на узком экране (перенос тегов), «Назад» по цепочке до сворачивания приложения
