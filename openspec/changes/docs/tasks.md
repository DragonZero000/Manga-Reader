## 1. Проверка документации (тест)

- [x] 1.1 `internal/archtest/docs_test.go` по D5: поиск корня репозитория; парность `docs/en` ↔ `docs/ru`, `README.md` ↔ `README.ru.md`, `CONTRIBUTING.md` ↔ `CONTRIBUTING.ru.md`; переключатель языка в первых 5 строках; относительные ссылки и изображения вне блоков кода указывают на существующие файлы (плюс `THIRD_PARTY_NOTICES.md`)
- [x] 1.2 Юнит-тесты разбора ссылок на строках-примерах: ссылка, изображение, `<img src>`, внешняя ссылка, якорь, ссылка с `#якорем`, `%20`, ссылка внутри блока ``` и в `inline code` не учитывается
- [x] 1.3 Обновить описание пакета `internal/archtest` («архитектурные границы и структура документации»)

## 2. Руководство пользователя

- [x] 2.1 `docs/ru/user-guide.md`: библиотека и папка (ПК — портативный режим, Android — выбор папки, смена папки, копирование через adb); экран «Библиотека» (сетка, обновление, автосканирование); формат архива и таблица полей `meta.json` (по `internal/library` и `testdata/example.zip`); страница произведения; читалка (режимы, тап-зоны, свайп, клавиши, увеличение, лента, панель, «Назад»/Esc); поиск и синтаксис с примерами (по `search-syntax` и справке в приложении); встроенный браузер Windows и Android; вкладка «Ошибки»; настройки. Названия элементов сверить с `internal/ui`
- [x] 2.2 `docs/en/user-guide.md` — перевод по D3 (русские названия элементов с переводом в скобках при первом упоминании); структура заголовков совпадает с русской версией

## 3. Документация для разработчиков

- [x] 3.1 `docs/ru/building.md`: требования (Go, gcc, Android Studio, SDK/NDK), команды `make`, сборка Android (переменные, архитектуры, подпись debug/release, 16 КБ), версия в `FyneApp.toml`, встроенный браузер для разработки (`make browser`), выпуск релиза, подробная инструкция по ключу и секретам (создание, проверка `keytool -list -v`, таблица секретов и переменной, `gh secret set` и веб-интерфейс, `certutil` — нельзя, сверка SHA-256, резервная копия), CI
- [x] 3.2 `docs/ru/architecture.md`: карта пакетов (`cmd/`, `internal/*`, `tools/*`, `android/`) с назначением; границы (`internal/archtest`); build-теги и заглушки; потоки UI и `fyne.Do`; встроенный браузер Windows (портативный Firefox, профиль и `user.js`, расширение-мост на `127.0.0.1`, привязка окна, реестр, NTFS-поток источника) и Android (GeckoView, Kotlin, JNI через `internal/mobilebrowser`, загрузки в SAF); версия; CI и релизы; `openspec/`
- [x] 3.3 `docs/en/building.md`, `docs/en/architecture.md` — переводы с той же структурой заголовков

## 4. README и CONTRIBUTING

- [x] 4.1 `README.ru.md` по требованию «README как витрина»: абзац о проекте, возможности, скриншоты (если есть), пометка о языке интерфейса, «Скачать» (файлы релиза, суммы), установка на Windows и Android (обновление поверх, разовое удаление самостоятельно собранной версии), системные требования (Windows 10/11 x64, Android 11+ arm64, Linux пока нет), для разработчиков (коротко + ссылки), лицензия
- [x] 4.2 `README.md` — английская версия
- [x] 4.3 `CONTRIBUTING.ru.md` и `CONTRIBUTING.md`: issue и pull request, `make test` и CI, процесс OpenSpec (`openspec/` в репозитории, папки агентов у каждого свои — `openspec init`), правило двух языков документации, строки UI на русском (до i18n), новый пакет → archtest и `architecture.md`, распространяемые компоненты → `THIRD_PARTY_NOTICES.md`, версия и релизы — только у мейнтейнера

## 5. Скриншоты

- [ ] 5.1 Автор делает скриншоты по D6 (без личных данных) и кладёт в `docs/images/`; вставить их в README (2–3) и `user-guide.md` на обоих языках. Если скриншотов нет — задача остаётся открытой, документы работают без них

## 6. Проверка

- [x] 6.1 `make test` проходит (включая новый тест документации); намеренно сломанная ссылка и удалённый перевод дают понятную ошибку, после отката тест снова проходит
- [x] 6.2 Сверка фактов: каждая команда в `building.md` выполнена или проверена по Makefile и workflow; названия кнопок и разделов в `user-guide.md` найдены в `internal/ui` (`grep`)
- [x] 6.3 Сверка языков: списки заголовков `docs/en/*` и `docs/ru/*`, `README*`, `CONTRIBUTING*` совпадают по структуре
- [ ] 6.4 Просмотр на GitHub после push: переключатели языка, таблицы, изображения и ссылки отображаются и работают
- [ ] 6.5 Автор задаёт About и Topics репозитория по D7 (`gh repo edit`)
